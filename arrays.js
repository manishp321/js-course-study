//  using arrays we can store multiplke values at one variable
var names= ['manish', 'shishir', 'narayan', 'gita' ,'lila'];
console.log(names);
// for specific names from the variables we can use
console.log(names[3]);
// for knowing the number of items inside the variable
console.log('number of items are'+ ' ' +names.length);